[settings]
; Limit the amount of update events within a set timeframe
; "Allow <throttle-limit> updates within <throttle-ms> of time"
throttle-limit = 3
throttle-ms = 60

;==============================================================================

[bar/main]
monitor = eDP1
dock = false

; Put the bar at the bottom of the screen
bottom = false

; Omit the % to specify the width in pixels
width = 100%
height = 30

; Offset defined in pixels
offset-x = 0
offset-y = 0

; Colors
background = #201017
foreground = #C2C8C6
linecolor = #A05369

; Borders
border-size = 0

; Used as a multiplier when adding spaces between elements
spacing = 0

; Height of underline and overline
lineheight = 3

; Number of spaces to add at the beginning/end of the bar
padding-left = 0
padding-right = 0

; Number of spaces to add before/after each module
module-margin-left = 2
module-margin-right = 0

; Fonts are defined using <FontName>;<Offset>
font-0 = Misc Tamsyn:size=10:antialias=true;0
font-1 = FontAwesome:size=10.2:antialias=true;0

; Locale used to localize module output
locale = sv_SE.UTF-8

; Position of the tray container
; If undefined, tray support will be disabled
;tray-position = right

; Restack the bar window and put it above the window manager's root
; Fixes the issue where the bar is being drawn over fullscreen windows
;wm-restack = i3

; Define what modules to output
modules-left = i3 mpd pianobar
;modules-center = label
modules-right = volume cpu cpuTemp memory wireless-network battery date time

;==============================================================================

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC

; This is useful in case the battery never reports 100% charge
;full-at = 99

; Seconds between reading battery capacity
poll-interval = 3

; Available tags:
;   <label-charging> (default)
;   <bar-capacity>
;   <ramp-capacity>
;   <animation-charging>
format-charging = <animation-charging> <label-charging>
format-charging-underline = #201017
format-charging-padding = 0

; Available tags:
;   <label-discharging> (default)
;   <bar-capaity>
;   <ramp-capacity>
format-discharging = <ramp-capacity> <label-discharging>
format-discharging-underline = #201017
format-discharging-padding = 0

; Available tags:
;   <label-full> (default)
;   <bar-capaity>
;   <ramp-capacity>
;format-full = <ramp-capacity> <label-full>
;format-full-underline = #201017
;format-full-padding = 0

; Available tokens:
;   %percentage% (default)
label-charging = %percentage%
label-charging-underline = #201017
label-charging-padding = 1

; Available tokens:
;   %percentage% (default)
label-discharging = %percentage%
label-discharging-underline = #201017
label-discharging-padding = 1

; Available tokens:
;   %percentage% (default)
label-full = Fully charged
label-full-underline = #201017
label-full-padding = 1

; Only applies if <ramp-capacity> is used
ramp-capacity-0 = 
ramp-capacity-1 = 
ramp-capacity-2 = 
ramp-capacity-3 = 
ramp-capacity-4 = 

; Only applies if <bar-capacity> is used
bar-capacity-width = 10

; Only applies if <animation-charging> is used
animation-charging-0 = 
animation-charging-1 = 
animation-charging-2 = 
animation-charging-3 = 
animation-charging-4 = 

; Framerate in milliseconds
animation-charging-framerate = 750

;==============================================================================

[module/cpu]
type = internal/cpu

; Seconds to sleep between updates
interval = 1

; Available tags:
;   <label> (default)
;   <bar-load>
;   <ramp-load>
;   <ramp-coreload>
format = <ramp-load> <label>
format-underline = #201017
format-padding = 0

; Available tokens:
;   %percentage% (default) - total cpu load
label = %percentage%
label-underline = #201017
label-padding = 1

ramp-load-0 = 
;ramp-coreload-0 = ▁
;ramp-coreload-1 = ▂
;ramp-coreload-2 = ▃
;ramp-coreload-3 = ▄
;ramp-coreload-4 = ▅
;ramp-coreload-5 = ▆
;ramp-coreload-6 = ▇
;ramp-coreload-7 = █

;==============================================================================

[module/cpuTemp]
type = custom/script

; Available tokens:
;   %counter%
; Command to be executed (using "/usr/bin/env sh -c [command]")
exec = perl ~/.config/i3/scripts/temperature

; Will the script output continous content?
tail = false

; Seconds to sleep between updates
interval = 3

; Limit the length of the output string
maxlen = 7

; Add trailing ellipsis when truncating the string
ellipsis = true

; Available tags:
;   <output> (default)
;format = <output>
format-background = #201017
format-foreground = #C2C8C6
format-underline = #201017
format-padding = 0

;==============================================================================

[module/i3]
type = internal/i3

; Only show workspaces defined on the same output as the bar
;
; Useful if you want to show monitor specific workspaces
; in different bars
;
; Default: false
pin-workspaces = true

; This will split the workspace name on ':'
; Default: false
strip-wsnumbers = true

; Limit the amount of chars to output for each workspace name
; Default: 0
wsname-maxlen = 0

; Sort the workspaces by index instead of the default
; sorting that groups the workspaces by output
; Default: false
index-sort = true

; ws-icon-[0-9]+ = label;icon
ws-icon-0 = term;
ws-icon-1 = code;
ws-icon-2 = web;
ws-icon-3 = mail;
ws-icon-4 = media;
ws-icon-5 = doc;
ws-icon-6 = irc;
ws-icon-7 = ftp;
ws-icon-8 = misc;
ws-icon-default = 

; Available tags:
;   <label-state> (default) - gets replaced with <label-(focused|unfocused|visible|urgent)>
;format = <label-state>

; Available tokens:
;   %name%
;   %icon%
;   %index%
;   %output%
; Default: %icon%  %name%
label-focused = %name%
label-focused-foreground = #C2C8C6
label-focused-background = #201017
label-focused-underline = #A05369
label-focused-padding = 2

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
label-unfocused =
;label-unfocused-padding = 2

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
label-visible =
label-visible-underline = #7366A0
label-visible-padding = 2

; Available tokens:
;   %name%
;   %icon%
;   %index%
; Default: %icon%  %name%
label-urgent = %name%
label-urgent-foreground = #201017
label-urgent-background = #7366A0
label-urgent-padding = 2

;==============================================================================

[module/date]
type = internal/date
date = %a %d %b

; Seconds to sleep between updates
interval = 1

; Available tags:
;   <date> (default)
format =   <date>
format-underline = #201017
format-padding = 0

;==============================================================================

[module/time]
type = internal/date
date = %H:%M

; Seconds to sleep between updates
interval = 1

; Available tags:
;   <date> (default)
format =   <date>
format-underline = #201017
format-padding = 0

;==============================================================================

[module/memory]
type = internal/memory

; Seconds to sleep between updates
interval = 3

; Available tags:
;   <label> (default)
;   <bar-used>
;   <bar-free>
format = <label>

; Available tokens:
;   %percentage_used% (default)
;   %percentage_free%
;   %gb_used%
;   %gb_free%
;   %gb_total%
;   %mb_used%
;   %mb_free%
;   %mb_total%
label =  %gb_used%
label-underline = #201017
label-padding = 0

; Only applies if <bar-used> is used
;bar-used-width = 50
;bar-used-foreground-0 = #55aa55
;bar-used-foreground-1 = #557755
;bar-used-foreground-2 = #f5a70a
;bar-used-foreground-3 = #ff5555
;bar-used-fill = ▐
;bar-used-empty = ▐
;bar-used-empty-foreground = #4444

;==============================================================================

[module/mpd]
type = internal/mpd

host = 127.0.0.1
port = 6600

; Seconds to sleep between progressbar/song timer sync
interval = 2

; Available tags:
;   <label-song> (default)
;   <label-time>
;   <bar-progress>
;   <toggle> - gets replaced with <icon-(pause|play)>
;   <icon-random>
;   <icon-repeat>
;   <icon-repeatone>
;   <icon-prev>
;   <icon-stop>
;   <icon-play>
;   <icon-pause>
;   <icon-next>
;   <icon-seekb>
;   <icon-seekf>
format-online = <label-song>

; Available tags:
;   <label-offline>
format-offline = <label-offline>

; Available tokens:
;   %artist%
;   %album%
;   %title%
; Default: %artist% - %title%
label-song = %artist% - %title%

; Available tokens:
;   %elapsed%
;   %total%
; Default: %elapsed% / %total%
;label-time = %elapsed% / %total%

; Available tokens:
;   None
label-offline = mpd is offline

; Only applies if <icon-X> is used
;icon-play = ⏵
;icon-pause = ⏸
;icon-stop = ⏹
;icon-prev = ⏮
;icon-next = ⏭
;icon-seekb = ⏪
;icon-seekf = ⏩
;icon-random = 🔀
;icon-repeat = 🔁
;icon-repeatone = 🔂

; Used to display the state of random/repeat/repeatone
; Only applies if <icon-[random|repeat|repeatone]> is used
;toggle-on-foreground = #ff
;toggle-off-foreground = #55

; Only applies if <bar-progress> is used
;bar-progress-width = 45
;bar-progress-indicator = |
;bar-progress-fill = ─
;bar-progress-empty = ─

;==============================================================================

[module/pianobar]
type = custom/script

; Available tokens:
;   %counter%
; Command to be executed (using "/usr/bin/env sh -c [command]")
exec = echo "$(cat ~/.config/pianobar/nowplaying)"

; Will the script output continous content?
tail = false

; Seconds to sleep between updates
interval = 3

; Limit the length of the output string
maxlen = 0

; Add trailing ellipsis when truncating the string
ellipsis = true

; Available tags:
;   <output> (default)
;format = <output>
format-background = #201017
format-foreground = #C2C8C6
format-padding = 0

; Available tokens:
;   %counter%
;
; "click-(left|middle|right)" will be executed using "/usr/bin/env sh -c [command]"
;click-left = echo left %counter%
;click-middle = echo middle %counter%
;click-right = echo right %counter%

; Available tokens:
;   %counter%
;
; "scroll-(up|down)" will be executed using "/usr/bin/env sh -c [command]"
;scroll-up = echo scroll up %counter%
;scroll-down = echo scroll down %counter%

;==============================================================================

[module/wireless-network]
type = internal/network
interface = wlp2s0

; Seconds to sleep between updates
interval = 3.0

; Test connectivity every Nth update
; A value of 0 disables the feature
; Default: 0
;ping-interval = 3

; Minimum output width of upload/download rate
; Default: 3
udspeed-minwidth = 3

; Available tags:
;   <label-connected> (default)
;   <ramp-signal>
format-connected = <label-connected>
format-connected-underline = #201017
format-conected-padding = 0

; Available tags:
;   <label-disconnected> (default)
format-disconnected = <label-disconnected>
format-disconnected-underline = #201017
format-disconnected-padding = 0

; Available tags:
;   <label-connected> (default)
;   <label-packetloss>
;   <animation-packetloss>
format-packetloss = <label-connected>

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %upspeed%   [wireless+wired]
;   %downspeed% [wireless+wired]
;   %linkspeed% [wired]
; Default: %ifname% %local_ip%
label-connected =  %essid%
label-connected-foreground = #C2C8C6
label-connected-underline = #201017
label-connected-padding = 0

; Available tokens:
;   %ifname%    [wireless+wired]
; Default: (none)
label-disconnected = not connected
label-disconnected-foreground = #C2C8C6
label-disconnected-underline = #201017
label-disconnected-padding = 0

; Available tokens:
;   %ifname%    [wireless+wired]
;   %local_ip%  [wireless+wired]
;   %essid%     [wireless]
;   %signal%    [wireless]
;   %linkspeed% [wired]
; Default: (none)
;label-packetloss = %essid%
;label-packetloss-foreground = #C2C8C6

; Only applies if <ramp-signal> is used
;ramp-signal-0 = 😱
;ramp-signal-1 = 😠
;ramp-signal-2 = 😒
;ramp-signal-3 = 😊
;ramp-signal-4 = 😃
;ramp-signal-5 = 😈

; Only applies if <animation-packetloss> is used
;animation-packetloss-0 = ⚠
;animation-packetloss-0-foreground = #ffa64c
;animation-packetloss-1 = 📶
;animation-packetloss-1-foreground = #000000
; Framerate in milliseconds
;animation-packetloss-framerate = 500

;==============================================================================

[module/volume]
type = internal/volume

; Name of the master mixer
; Default: Master
master-mixer = Master

; Optionally define speaker and headphone mixers
; Use the following command to list available mixer controls:
; $ amixer scontrols | sed -nr "s/.*'([[:alnum:]]+)'.*/\1/p"
; Default: none
speaker-mixer = Speaker
; Default: none
;headphone-mixer = Headphone

; NOTE: This is required if headphone_mixer is defined
; Use the following command to list available device controls
; $ amixer controls | sed -r "/CARD/\!d; s/.*=([0-9]+).*name='([^']+)'.*/printf '%3.0f: %s\n' '\1' '\2'/e" | sort
; Default: none
;headphone-id =

; Available tags:
;   <label-volume> (default)
;   <ramp-volume>
;   <bar-volume>
format-volume = <ramp-volume> <label-volume>
format-volume-underline = #201017
format-volume-padding = 0

; Available tags:
;   <label-muted> (default)
;   <ramp-volume>
;   <bar-volume>
format-muted = <label-muted>
format-muted-underline = #201017
format-muted-padding = 0

; Available tokens:
;   %percentage% (default)
label-volume = %percentage%
label-volume-underline = #201017
label-volume-padding = 1

; Available tokens:
;   %percentage% (default)
label-muted =  muted
label-muted-foreground = #C2C8C6
label-muted-underline = #201017
label-muted-padding = 1

; Only applies if <ramp-volume> is used
ramp-volume-0 = 
ramp-volume-1 = 
ramp-volume-2 = 

; If defined, it will replace <ramp-volume> when
; headphones are plugged in to `headphone_control_numid`
; If undefined, <ramp-volume> will be used for both
; Only applies if <ramp-volume> is used
;ramp-headphones-0 = 
;ramp-headphones-1 = 